<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" role="img" aria-label="Competition Math Reference">
  <rect width="64" height="64" rx="14" fill="#0f1420"/>
  <!-- Sigma drawn as a stroked path rather than a <text> glyph: a favicon is rendered
       outside the page, where the site's serif stack is not guaranteed to resolve. -->
  <path d="M45 16H20l14 16-14 16h25" fill="none" stroke="#f5c451" stroke-width="5.5"
        stroke-linecap="round" stroke-linejoin="round"/>
</svg>
